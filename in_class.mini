struct A {
   int x;
   int y;
};

fun f(int x, struct A a) struct A {
   struct A na;
   na = new A;

   if (x > 0) {
      na.x = a.x + x;
   } else {
      na.x = 0;
   } 
   na.y = a.y;
   return na;
}

fun main() int {
   return 0;
}
